ansible:
    - component: shop.component
      hosting: os.compute
      weight: 1

    - component: shop.component
      hosting: gcp.appengine
      weight: 0.5

    - component: os.compute
      weight: 0.5

    - component: gcp.appengine
      weight: 0.5

    - component: gcp.cloudsql
      weight: 0.5

    - component: mysql.database
      hosting: gcp.cloudsql
      weight: 0.5

    - component: mysql.database
      hosting: mysql.dbms
      weight: 1

    - component: mysql.dbms
      hosting: os.compute
      weight: 1

terraform:
    - component: shop.component
      hosting: os.compute
      weight: 0

    - component: shop.component
      hosting: gcp.appengine
      weight: 1

    - component: os.compute
      weight: 1

    - component: gcp.appengine
      weight: 1

    - component: gcp.cloudsql
      weight: 1

    - component: mysql.database
      hosting: gcp.cloudsql
      weight: 1

    - component: mysql.database
      hosting: mysql.dbms
      weight: 0

    - component: mysql.dbms
      hosting: os.compute
      weight: 0

kubernetes:
    - component: shop.component
      hosting: kubernetes
      weight: 1

    - component: shop.component.expose
      hosting: kubernetes
      weight: 1

    - component: mysql.dbms
      hosting: kubernetes
      weight: 1

    - component: mysql.dbms.expose
      hosting: kubernetes
      weight: 1

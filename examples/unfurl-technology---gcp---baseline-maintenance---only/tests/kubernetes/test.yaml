expected: ../../../unfurl-technology---gcp---plus-maintenance-automated---only/tests/kubernetes/expected.yaml
inputs: ../../../unfurl-technology---gcp---plus-maintenance-automated---only/tests/kubernetes/inputs.yaml

replace:
    - - frontend_service_kubernetes
      - frontend_service

    - - frontend_ingress_kubernetes
      - frontend_ingress

    - - checkout_service_kubernetes
      - checkout_service

    - - email_service_kubernetes
      - email_service

    - - payment_service_kubernetes
      - payment_service

    - - currency_service_kubernetes
      - currency_service

    - - shipping_service_kubernetes
      - shipping_service

    - - cart_service_kubernetes
      - cart_service

    - - mysql_database_ansible
      - mysql_database

    - - mysql_dbms_kubernetes
      - mysql_dbms

    - - product_service_kubernetes
      - product_service

    - - recommendation_service_kubernetes
      - recommendation_service

    - - advertisement_service_kubernetes
      - advertisement_service

    - - analytical_service_kubernetes
      - analytical_service

merge:
    topology_template:
        inputs:
            database_password:
                type: string

            dbms_password:
                type: string

            os_network:
                type: string

            os_ssh_key_name:
                type: string

            os_ssh_user:
                type: string

            os_ssh_key_file:
                type: string

            os_region_name:
                type: string

            os_auth_type:
                type: string

            os_auth_url:
                type: string

            os_identity_api_version:
                type: string

            os_interface:
                type: string

            os_application_credential_id:
                type: string

            os_application_credential_secret:
                type: string

            gcp_region:
                type: string

            gcp_service_account_file:
                type: string

            gcp_project:
                type: string

            k8s_api_address:
                type: string

            k8s_api_port:
                type: string

            k8s_ca_cert_file:
                type: string

            k8s_client_cert_file:
                type: string

            k8s_client_key_file:
                type: string

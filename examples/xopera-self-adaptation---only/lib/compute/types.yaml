tosca_definitions_version: tosca_simple_yaml_1_3

node_types:
    compute.raspberry:
        derived_from: tosca.nodes.Compute
        properties:
            ssh_user:
                type: string

            ssh_key_file:
                type: string

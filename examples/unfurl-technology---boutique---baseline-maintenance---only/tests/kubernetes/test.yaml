expected: ../../../unfurl-technology---boutique---plus-maintenance-automated---only/tests/kubernetes/expected.yaml
inputs: ../../../unfurl-technology---boutique---plus-maintenance-automated---only/tests/kubernetes/inputs.yaml

replace:
    - - frontend_component_kubernetes
      - frontend_component

    - - frontend_ingress_kubernetes
      - frontend_ingress

    - - checkout_component_kubernetes
      - checkout_component

    - - email_component_kubernetes
      - email_component

    - - payment_component_kubernetes
      - payment_component

    - - currency_component_kubernetes
      - currency_component

    - - shipping_component_kubernetes
      - shipping_component

    - - cart_component_kubernetes
      - cart_component

    - - cart_database_ansible
      - cart_database

    - - cart_dbms_kubernetes
      - cart_dbms

    - - product_component_kubernetes
      - product_component

    - - recommendation_component_kubernetes
      - recommendation_component

    - - advertisement_component_kubernetes
      - advertisement_component

    - - analytical_component_kubernetes
      - analytical_component

    - - mysql.database::terraform::mysql
      - mysql.database::terraform::mysql.dbms::kubernetes

    - - mysql.database::ansible::mysql
      - mysql.database::ansible::mysql.dbms::kubernetes

merge:
    topology_template:
        inputs:
            database_password:
                type: string

            dbms_password:
                type: string

            os_network:
                type: string

            os_ssh_key_name:
                type: string

            os_ssh_user:
                type: string

            os_ssh_key_file:
                type: string

            os_region_name:
                type: string

            os_auth_type:
                type: string

            os_auth_url:
                type: string

            os_identity_api_version:
                type: string

            os_interface:
                type: string

            os_application_credential_id:
                type: string

            os_application_credential_secret:
                type: string

            gcp_region:
                type: string

            gcp_service_account_file:
                type: string

            gcp_project:
                type: string

            k8s_host:
                type: string

            k8s_ca_cert_file:
                type: string

            k8s_client_cert_file:
                type: string

            k8s_client_key_file:
                type: string

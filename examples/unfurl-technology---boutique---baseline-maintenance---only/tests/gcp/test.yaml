expected: ../../../unfurl-technology---boutique---plus-maintenance-automated---only/tests/gcp/expected.yaml
inputs: ../../../unfurl-technology---boutique---plus-maintenance-automated---only/tests/gcp/inputs.yaml

replace:
    - - frontend_component_terraform
      - frontend_component

    - - checkout_component_terraform
      - checkout_component

    - - email_component_terraform
      - email_component

    - - payment_component_terraform
      - payment_component

    - - currency_component_terraform
      - currency_component

    - - shipping_component_terraform
      - shipping_component

    - - cart_component_terraform
      - cart_component

    - - cart_database_terraform
      - cart_database

    - - cart_dbms_terraform
      - cart_dbms

    - - product_component_terraform
      - product_component

    - - recommendation_component_terraform
      - recommendation_component

    - - advertisement_component_terraform
      - advertisement_component

    - - analytical_component_terraform
      - analytical_component

    - - mysql.database::terraform::mysql
      - mysql.database::terraform::mysql.gcp

merge:
    topology_template:
        inputs:
            database_password:
                type: string

            dbms_password:
                type: string

            os_network:
                type: string

            os_ssh_key_name:
                type: string

            os_ssh_user:
                type: string

            os_ssh_key_file:
                type: string

            os_region_name:
                type: string

            os_auth_type:
                type: string

            os_auth_url:
                type: string

            os_identity_api_version:
                type: string

            os_interface:
                type: string

            os_application_credential_id:
                type: string

            os_application_credential_secret:
                type: string

            gcp_region:
                type: string

            gcp_service_account_file:
                type: string

            gcp_project:
                type: string

            k8s_host:
                type: string

            k8s_ca_cert_file:
                type: string

            k8s_client_cert_file:
                type: string

            k8s_client_key_file:
                type: string

        node_templates:
            analytical_component:
                type: analytical.component::terraform::gcp
                properties:
                    optional_analytical_feature: false
                    premium_analytical_feature: false
                requirements:
                    - checkout: checkout_component
                    - recommendation: recommendation_component
                    - host: gcp_cloudrun

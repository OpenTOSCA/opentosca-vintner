tosca_definitions_version: tosca_simple_yaml_1_3

topology_template:
    node_templates:
        shop:
            type: shop.component.terraform
            requirements:
                - database: database
                - host: gcp_runtime

        database:
            type: mysql.database.terraform
            requirements:
                - host: gcp_dbms

        gcp_dbms:
            type: gcp.cloud_sql.terraform

        gcp_runtime:
            type: gcp.app_engine.terraform

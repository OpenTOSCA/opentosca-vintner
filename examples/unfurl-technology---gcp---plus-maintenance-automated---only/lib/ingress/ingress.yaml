tosca_definitions_version: tosca_simple_yaml_1_3

imports:
    - ../base.yaml

node_types:
    ###################################################
    #
    # Abstract
    #
    ###################################################

    ingress:
        derived_from: root

        requirements:
            - upstream:
                  capability: tosca.capabilities.Root
                  relationship: tosca.relationships.ConnectsTo

            - host:
                  capability: tosca.capabilities.Compute
                  relationship: tosca.relationships.HostedOn

    # TODO: ingress.ansible.openstack
    # TODO: ingress.terraform.openstack

    # TODO: ingress.ansible.kubernetes
    # TODO: ingress.terraform.kubernetes
    # TODO: ingress.kubernetes.kubernetes

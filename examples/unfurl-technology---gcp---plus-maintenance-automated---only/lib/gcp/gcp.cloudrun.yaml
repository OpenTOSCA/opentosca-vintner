tosca_definitions_version: tosca_simple_yaml_1_3

imports:
    - ../base.yaml

node_types:
    ###################################################
    #
    # Abstract
    #
    ###################################################

    gcp.cloudrun:
        derived_from: gcp.service

        capabilities:
            host:
                type: tosca.capabilities.Compute

    # TODO: gcp.cloudrun.ansible.gcp
    # TODO: gcp.cloudrun.terraform.gcp

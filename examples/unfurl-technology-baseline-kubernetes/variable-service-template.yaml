tosca_definitions_version: tosca_simple_yaml_1_3

topology_template:
    node_templates:
        shop:
            type: shop.component.helm
            requirements:
                - database: database
                - host: kubernetes

        database:
            type: mysql.database.ansible
            requirements:
                - host: mysql_dbms

        mysql_dbms:
            type: mysql.dbms.helm
            requirements:
                - host: kubernetes

        kubernetes:
            type: kubernetes

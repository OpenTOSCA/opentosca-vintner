# <%- test.config.name || test.id %>

{{ autogenerated_notice('./task docs:generate:tests:variability') }}

<% if (test.config.description) { -%>
## Description

<%- test.config.description %>
<% } %>
## Variable Service Template

The variability of the following variable service template shall be resolved.

```yaml linenums="1"
{% raw %}
<%- utils.toYAML(test.template) -%>
{% endraw %}
```

<% if (!utils.isEmpty(test.inputs)) { -%>
## Variability Inputs

When resolving variability, the following variability inputs shall be used.

```yaml linenums="1"
<%- utils.toYAML(test.inputs) -%>
```
<% } -%>

<% if (test.config?.preset) { -%>
## Variability Preset

The following variability preset is used.

```yaml linenums="1"
<%- utils.toYAML(test.config.preset) -%>
```
<% } -%>

<% if (test.config.replace) { -%>
## Replace

The following strings are searched and replaced in the variability-resolved service template.

```yaml linenums="1"
<%- utils.toYAML(test.config.replace) -%>
```
<% } -%>

<% if (test.expected) { -%>
## Variability-Resolved Service Template

The following variability-resolved service template is expected.

```yaml linenums="1"
{% raw %}
<%- utils.toYAML(test.expected) -%>
{% endraw %}
```
<% } -%>

<% if (test.config?.error) { -%>
## Expected Error

The following error is expected to be thrown, when resolving variability.

```text linenums="1"
<%- utils.toYAML(test.config.error) -%>
```
<% } -%>

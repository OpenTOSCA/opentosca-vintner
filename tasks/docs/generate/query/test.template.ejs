# <%- test.config.name || test.id %>

{{ autogenerated_notice('./task docs:generate:tests:query') }}

<% if (test.config.description) { -%>
## Description

<%- test.config.description -%>
<% } %>
This test is of type `<%- test.config.type %>`.

## Service Template

The following service template is used.

```yaml linenums="1" title="<%- test.toFileTitle() -%>"
<%- utils.toYAML(test.template) -%>
```
<% if (test.config.query) { %>

## Query

The following query is executed.

```text linenums="1"
<%- test.config.query %>
```
<% } -%>
<% if (test.expected) { %>

## Query Result

The following query result is expected.

```yaml linenums="1"
<%- utils.toYAML(test.expected) -%>
```
<% } -%>
<% if (test.config?.error) { %>

## Expected Error

The following error is expected to be thrown.

```text linenums="1"
<%- utils.toYAML(test.config.error) -%>
```
<% } -%>

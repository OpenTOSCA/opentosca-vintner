<% Object.entries(template).filter(([key, _]) => key.endsWith('_types')).forEach(([types_key, types]) => { %>
### <%= utils.toTitle(types_key) %>

We specify the following <%= utils.toText(types_key) %>.
<% Object.entries(types).forEach(([type_key, type]) => { %>
#### <%= type_key %>

<%= type.description %>

```yaml linenums="1"
<%- type_key %>:
<%- utils.toYAML(type).split('\n').map(it => '    ' + it).slice(0, -1).join('\n') -%>

```
<% }) -%>
<% }) -%>



tosca_definitions_version: tosca_simple_yaml_1_3

topology_template:
    node_templates:
        node_one:
            type: node_one

    policies:
        - policy_one:
              type: policy_one
              targets: [node_one]

        - policy_three:
              type: policy_three
              targets: [node_one]

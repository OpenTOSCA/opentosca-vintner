tosca_definitions_version: tosca_simple_yaml_1_3

topology_template:
    node_templates:
        one:
            type: one

    policies:
        - pone:
              type: pone
              targets: [one]

        - pthree:
              type: pthree
              targets: [one]

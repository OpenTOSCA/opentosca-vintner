tosca_definitions_version: tosca_variability_1_0

topology_template:
    node_templates:
        one:
            type: one
            conditions: {get_element_presence: two}

        two:
            type: two
            conditions: {get_element_presence: one}

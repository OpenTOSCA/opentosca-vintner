import PackageManager from '../../../src/package-manager'
import * as files from '../../../src/utils/files'
import {expect} from 'chai'

it('install', async () => {
    // TODO: setup custom filesystem
    //await PackageManager.install()
    // TODO: check stuff
    //expect(files.exists('clone dir')).to.be.true
    //expect(files.exists('worktree dir')).to.be.true
    //expect(files.exists('library dir')).to.be.true
    //await PackageManager.purge()
})

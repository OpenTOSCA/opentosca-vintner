description: |
    - Under the condition, "node_presence" function checks if the node is present or not.
    - node is present since true is assigned as the condition.
    - Container is present since the "node_presence" function returns true in the condition.
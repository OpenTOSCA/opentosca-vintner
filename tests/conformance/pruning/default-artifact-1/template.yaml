tosca_definitions_version: tosca_variability_1_0_rc_3

topology_template:
    variability:
        options:
            artifact_default_condition_mode: default
    node_templates:
        node_one:
            type: node_one
            managed: false
            artifacts:
                - key_one:
                      type: any
                      file: value_one_three

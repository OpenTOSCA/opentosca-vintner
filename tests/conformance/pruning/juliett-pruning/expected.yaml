tosca_definitions_version: tosca_simple_yaml_1_3

topology_template:
    node_templates:
        source:
            type: source

        target:
            type: target

        another:
            type: another
            requirements:
                - another: target

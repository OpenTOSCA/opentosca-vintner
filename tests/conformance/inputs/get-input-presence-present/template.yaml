tosca_definitions_version: tosca_variability_1_0

topology_template:
    inputs:
        input:
            conditions: true
            type: string

    node_templates:
        container:
            type: container
            conditions: {get_input_presence: input}

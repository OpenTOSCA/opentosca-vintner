description: |
    - input "one" is present since true is the default condition.
    - input "two" is absent since false is assigned as condition.
    - input "three" is present since true is assigned as condition.
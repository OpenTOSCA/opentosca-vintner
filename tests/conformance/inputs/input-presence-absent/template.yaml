tosca_definitions_version: tosca_variability_1_0

topology_template:
    inputs:
        input:
            conditions: false
            type: string

    node_templates:
        container:
            type: container
            conditions: {input_presence: input}

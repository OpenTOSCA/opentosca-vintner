tosca_definitions_version: tosca_variability_1_0

topology_template:
    node_templates:
        container:
            type: container
            conditions: {artifact_presence: [container, 0]}
            artifacts:
                artifact_one:
                    conditions: true
                    file: path

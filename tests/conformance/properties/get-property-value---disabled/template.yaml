tosca_definitions_version: tosca_variability_1_0

topology_template:
    node_templates:
        node_one:
            type: node_one
            properties:
                - key_one:
                      expression: {get_property_value: [node_two, property_one]}

        node_two:
            type: node_two
            properties:
                - property_one:
                      value: 1
                      conditions: false
                - property_one:
                      value: 2
                      conditions: true

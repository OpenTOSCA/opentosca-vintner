tosca_definitions_version: tosca_variability_1_0

topology_template:
    node_templates:
        container:
            type: container
            conditions: {get_group_presence: group_one}

    groups:
        group_one:
            conditions: true
            members: []

tosca_definitions_version: tosca_variability_1_0

topology_template:
    node_templates:
        container:
            type: container
            conditions: {group_presence: group_one}

    groups:
        group_one:
            type: group_one
            conditions: false
            members: []

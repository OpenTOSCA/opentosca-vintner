tosca_definitions_version: tosca_variability_1_0

topology_template:
    node_templates:
        container:
            type: container
            properties:
                - value:
                      expression: {get_artifact_presence: [container, artifact_one]}
            artifacts:
                artifact_one:
                    conditions: false
                    file: path

tosca_definitions_version: tosca_variability_1_0

topology_template:
    node_templates:
        container:
            type: container
            conditions: {get_artifact_presence: [container, artifact_one]}
            artifacts:
                artifact_one:
                    conditions: true
                    file: path

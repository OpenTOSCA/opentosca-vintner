tosca_definitions_version: tosca_variability_1_0

topology_template:
    variability:
        options:
            type_default_condition: true
            output_default_condition: true
            property_default_condition: true

    outputs:
        input:
            type: string
            value: {eval: '::container::some_property'}

    node_templates:
        container:
            type: container
            conditions: false
            properties:
                some_property: some_value

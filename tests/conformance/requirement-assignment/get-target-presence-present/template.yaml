tosca_definitions_version: tosca_variability_1_0

topology_template:
    node_templates:
        container:
            type: container
            properties:
                - value:
                      expression: {get_relation_presence: [node_one, relation_one]}
        node_one:
            type: node_one
            requirements:
                - relation_one:
                      node: node_two
                      conditions: {get_target_presence: SELF}

        node_two:
            type: node_two
            conditions: false

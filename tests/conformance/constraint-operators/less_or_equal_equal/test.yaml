name: 'Operator "less_or_equal" Evaluates to "true" (Equal)'
description: |
    - The "less_or_equal" operator evaluates the expression.
    - Assigns "true" to the property "value" of the node "container" since both parameters are equal.

name: 'Operator "valid_values" Evaluates to "false"'
description: |
    - The "valid_values" operator evaluates the expression.
    - Assigns "false" to the property "value" of the node "container" since the parameter is not a valid value.

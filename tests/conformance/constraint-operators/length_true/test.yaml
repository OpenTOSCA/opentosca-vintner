name: 'Operator "length" Evaluates to "true"'
description: |
    - The "length" operator evaluates the expression.
    - Assigns "true" to the property "value" of the node "container" since the parameter has expected length.
